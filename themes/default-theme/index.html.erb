<div class="default-theme">
	<%= theme_render_partial '_navigation' %>

	<%= theme_render_partial '_heading' %>

	<div class="container">
		<% posts.each do |post| %>
			<%= render_post post %>
		<% end %>
	</div>

</div>